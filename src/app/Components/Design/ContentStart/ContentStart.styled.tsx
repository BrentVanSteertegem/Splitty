import { View } from 'react-native'
import styled from 'styled-components'
import { DefaultStyles } from '../../../style'

export const StDefaultContentStart = styled(View)`
    ${DefaultStyles.defaultContentStart}
`

export const StSmallContentStart = styled(View)`
    ${DefaultStyles.smallContentStart}
`