import { View } from 'react-native'
import styled from 'styled-components'

export const StBillItem = styled(View)`
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
`